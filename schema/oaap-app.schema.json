{
  "$schema": "https://json-schema.org/draft/2020-12/schema",
  "$id": "https://oaap.org/schema/oaap-app-0.2.json",
  "title": "OAAP app manifest (oaap-app.yaml), manifest versions 0.1 to 0.4",
  "$comment": "This schema is an AUTHORING tool: it rejects unknown fields so a typo is caught where it is made. A node in the field reads more tolerantly (RFC-0012 §8.2) — it accepts a higher minor version and ignores what it does not know, because otherwise every extension of the format would be a flag day for the whole fleet. Strict schema, tolerant runtime. The newest schema always validates every manifest version still readable. Until 2026-09-12 it skipped 0.3 (data_model/contributes/consumes, RFC-0031 Schritt 2): appctl.py accepted such manifests while this schema refused them, so every twin-aware app failed its own authoring check. The id keeps its historic '0.2' so existing references do not break. Check changes with 'python schema/validate.py'.",
  "type": "object",
  "required": [
    "oaap_manifest",
    "app"
  ],
  "additionalProperties": false,
  "allOf": [
    {
      "$comment": "A field added in 0.2 may only be used by a manifest that declares 0.2. The runtime does NOT enforce this — it reads whatever it finds — but an author who uses a new field while still claiming the old version has made a mistake worth catching here.",
      "if": {
        "properties": {
          "oaap_manifest": {
            "const": "0.1"
          }
        }
      },
      "then": {
        "properties": {
          "app": {
            "not": {
              "required": [
                "class"
              ]
            }
          }
        }
      }
    },
    {
      "$comment": "Same rule for the 0.3 sections (oaap.data.model 0.1 §2.2, RFC-0031 §5): data_model, contributes and consumes may only be used by a manifest declaring at least 0.3.",
      "if": {
        "properties": {
          "oaap_manifest": {
            "enum": [
              "0.1",
              "0.2"
            ]
          }
        }
      },
      "then": {
        "not": {
          "anyOf": [
            {
              "required": [
                "data_model"
              ]
            },
            {
              "required": [
                "contributes"
              ]
            },
            {
              "required": [
                "consumes"
              ]
            }
          ]
        }
      }
    },
    {
      "$comment": "Same rule for the 0.4 field: 'launchpad' may only be used by a manifest declaring at least 0.4 (RFC-0036).",
      "if": {
        "properties": {
          "oaap_manifest": {
            "enum": [
              "0.1",
              "0.2",
              "0.3"
            ]
          }
        }
      },
      "then": {
        "not": {
          "required": [
            "launchpad"
          ]
        }
      }
    },
    {
      "$comment": "Two shapes, told apart structurally exactly as appctl.py does (oaap.data.model 0.1 §2.8): a manifest with a 'data_model' section and NO 'services' key is a data_models ARTEFACT — types only, never a container. It has no app.type (that describes how a container is packaged), no routes/endpoints/storage/health (nothing to attach them to) and no contributes/consumes (those bind an INSTANCE, and an artefact never becomes one). Everything else is an app, and needs services, routes, health and app.type.",
      "if": {
        "required": [
          "data_model"
        ],
        "not": {
          "required": [
            "services"
          ]
        }
      },
      "then": {
        "properties": {
          "app": {
            "not": {
              "required": [
                "type"
              ]
            }
          }
        },
        "not": {
          "anyOf": [
            {
              "required": [
                "routes"
              ]
            },
            {
              "required": [
                "endpoints"
              ]
            },
            {
              "required": [
                "storage"
              ]
            },
            {
              "required": [
                "health"
              ]
            },
            {
              "required": [
                "contributes"
              ]
            },
            {
              "required": [
                "consumes"
              ]
            }
          ]
        }
      },
      "else": {
        "required": [
          "services",
          "routes",
          "health"
        ],
        "properties": {
          "app": {
            "required": [
              "type"
            ]
          }
        }
      }
    }
  ],
  "properties": {
    "oaap_manifest": {
      "enum": [
        "0.1",
        "0.2",
        "0.3",
        "0.4"
      ],
      "$comment": "MAJOR.MINOR. A new MINOR only adds; a new MAJOR is the only thing a node may refuse (RFC-0012 §8.2). Deliberately not the semver 0.x reading, where the minor would carry breaking changes. 0.2 adds app.class. 0.3 adds data_model/contributes/consumes (oaap.data.model 0.1 §2.2, RFC-0031 §5) and the data_models artefact shape (§2.8). 0.4 adds the 'launchpad' section (RFC-0036)."
    },
    "must_understand": {
      "type": "array",
      "items": {
        "type": "string",
        "pattern": "^[a-z][a-z0-9_]*$"
      },
      "$comment": "RFC-0012 §8.2 rule 2: features a reader MUST understand to handle this manifest correctly. A reader missing one refuses this app with a clear message instead of installing something half-understood — the exception that makes 'ignore what you do not know' safe. Only use it where ignoring the feature would produce a broken install; optional additions do not belong here."
    },
    "app": {
      "type": "object",
      "required": [
        "id",
        "name",
        "version"
      ],
      "$comment": "'type' is required for an app and forbidden for a data_models artefact — see the last allOf rule above.",
      "additionalProperties": false,
      "properties": {
        "id": {
          "type": "string",
          "pattern": "^[a-z0-9][a-z0-9-]{1,38}[a-z0-9]$"
        },
        "name": {
          "type": "string",
          "minLength": 1,
          "maxLength": 80
        },
        "version": {
          "type": "string",
          "pattern": "^\\d+\\.\\d+\\.\\d+([-+][0-9A-Za-z.-]+)?$"
        },
        "type": {
          "enum": [
            "native",
            "image",
            "wrapped"
          ]
        },
        "class": {
          "enum": [
            "frontend",
            "service"
          ],
          "$comment": "Manifest 0.2, RFC-0012 §1.2. What the app IS, as opposed to app.type, which says how it is PACKAGED. 'frontend' (the default when absent) has a user interface; 'service' is used by other software and therefore gets no launchpad tile (oaap.apps.runtime 2.10). It belongs in the manifest and not only in a store list, because a node must be able to answer the question offline, for an app installed straight from Git, and without a foreign list being able to change what its launchpad shows. Deliberately NOT a must_understand feature: a node that ignores it shows one tile too many, which is untidy, not broken.",
          "default": "frontend"
        },
        "description": {
          "type": "string"
        },
        "icon": {
          "type": "string"
        }
      }
    },
    "services": {
      "type": "object",
      "minProperties": 1,
      "additionalProperties": {
        "type": "object",
        "required": [
          "port"
        ],
        "additionalProperties": false,
        "properties": {
          "build": {
            "type": "string"
          },
          "image": {
            "type": "string"
          },
          "port": {
            "type": "integer",
            "minimum": 1,
            "maximum": 65535
          },
          "subpath": {
            "type": "boolean"
          }
        },
        "oneOf": [
          {
            "required": [
              "build"
            ]
          },
          {
            "required": [
              "image"
            ]
          }
        ]
      }
    },
    "routes": {
      "type": "array",
      "minItems": 1,
      "items": {
        "type": "object",
        "required": [
          "path",
          "roles"
        ],
        "additionalProperties": false,
        "properties": {
          "path": {
            "type": "string",
            "pattern": "^/"
          },
          "roles": {
            "type": "array",
            "minItems": 1,
            "items": {
              "enum": [
                "admin",
                "keyuser",
                "user",
                "guest",
                "partner",
                "public"
              ]
            }
          },
          "service": {
            "type": "string",
            "pattern": "^[a-z0-9][a-z0-9-]*$"
          }
        }
      }
    },
    "storage": {
      "type": "array",
      "items": {
        "type": "object",
        "required": [
          "name",
          "mount"
        ],
        "additionalProperties": false,
        "properties": {
          "name": {
            "type": "string",
            "pattern": "^[a-z0-9][a-z0-9-]*$"
          },
          "mount": {
            "type": "string",
            "pattern": "^/"
          },
          "service": {
            "type": "string",
            "pattern": "^[a-z0-9][a-z0-9-]*$"
          }
        }
      }
    },
    "config": {
      "type": "array",
      "items": {
        "type": "object",
        "required": [
          "key",
          "label"
        ],
        "additionalProperties": false,
        "properties": {
          "key": {
            "type": "string",
            "pattern": "^[A-Za-z][A-Za-z0-9_]*$"
          },
          "label": {
            "type": "string"
          },
          "default": {
            "type": "string"
          },
          "secret": {
            "type": "boolean"
          },
          "multiline": {
            "type": "boolean",
            "description": "The value is a LIST. The portal offers one line per entry; on the wire the lines are joined with ';' — so an app parses the single-line form and no entry may itself contain ';'."
          },
          "generate": {
            "enum": ["token"],
            "description": "Only for a key whose value THIS app defines: the portal offers to produce it, as 32 random bytes in url-safe base64 (alphabet [A-Za-z0-9_-], no padding) — a value the platform's line-based env file and ';'-separated lists can carry. Do NOT declare it on a key that holds somebody else's credential."
          }
        }
      }
    },
    "health": {
      "type": "object",
      "required": [
        "path"
      ],
      "additionalProperties": false,
      "properties": {
        "path": {
          "type": "string",
          "pattern": "^/"
        },
        "startup_grace_seconds": {
          "type": "integer",
          "minimum": 0,
          "maximum": 1800
        }
      }
    },
    "placement": {
      "type": "object",
      "additionalProperties": false,
      "properties": {
        "hints": {
          "type": "array",
          "items": {
            "type": "string"
          }
        }
      }
    },
    "data_model": {
      "type": "object",
      "additionalProperties": false,
      "description": "Manifest 0.3, oaap.data.model 0.1 §2.2 (RFC-0031 §5): the types THIS package ships — registered under origin app:<id> for an app, model:<id> for a data_models artefact. Every list is optional. Keys are checked for shape only; whether a type named in 'on'/'from'/'to'/'attributes' actually exists is decided at install, against the node's registry (§2.4), never here.",
      "properties": {
        "object_types": {
          "type": "array",
          "items": {
            "type": "object",
            "required": [
              "key"
            ],
            "additionalProperties": false,
            "properties": {
              "key": {
                "$ref": "#/$defs/type_key"
              },
              "title": {
                "type": "string"
              },
              "title_plural": {
                "type": "string"
              },
              "identifying": {
                "type": "array",
                "items": {
                  "$ref": "#/$defs/type_key"
                },
                "description": "Attribute keys used for duplicate detection (RFC-0031 §3.6)."
              },
              "description": {
                "type": "string"
              },
              "tags": {
                "$ref": "#/$defs/tags"
              }
            }
          }
        },
        "attribute_types": {
          "type": "array",
          "items": {
            "type": "object",
            "required": [
              "key",
              "value_type"
            ],
            "additionalProperties": false,
            "properties": {
              "key": {
                "$ref": "#/$defs/type_key"
              },
              "title": {
                "type": "string"
              },
              "value_type": {
                "enum": [
                  "text",
                  "int",
                  "decimal",
                  "bool",
                  "date",
                  "datetime",
                  "enum",
                  "ref"
                ]
              },
              "description": {
                "type": "string"
              },
              "tags": {
                "$ref": "#/$defs/tags"
              }
            }
          }
        },
        "group_types": {
          "type": "array",
          "items": {
            "type": "object",
            "required": [
              "key",
              "on"
            ],
            "additionalProperties": false,
            "properties": {
              "key": {
                "type": "string",
                "pattern": "^[a-z][a-z0-9]*(\\.[a-z][a-z0-9]*)+$",
                "description": "Dotted lowercase, <namespace>.<name> (e.g. crm.core, raci.assignments)."
              },
              "title": {
                "type": "string"
              },
              "on": {
                "$ref": "#/$defs/type_key",
                "description": "The object type this group attaches to. In YAML write it quoted (\"on\":) — unquoted, YAML 1.1 readers turn it into the boolean true."
              },
              "attributes": {
                "type": "array",
                "items": {
                  "$ref": "#/$defs/type_key"
                }
              },
              "relations": {
                "type": "array",
                "items": {
                  "$ref": "#/$defs/type_key"
                }
              },
              "activities": {
                "type": "array",
                "items": {
                  "$ref": "#/$defs/type_key"
                }
              },
              "description": {
                "type": "string"
              },
              "tags": {
                "$ref": "#/$defs/tags"
              }
            }
          }
        },
        "relation_types": {
          "type": "array",
          "items": {
            "type": "object",
            "required": [
              "key",
              "from",
              "to"
            ],
            "additionalProperties": false,
            "properties": {
              "key": {
                "$ref": "#/$defs/type_key"
              },
              "title": {
                "type": "string"
              },
              "title_inverse": {
                "type": "string"
              },
              "from": {
                "$ref": "#/$defs/type_key"
              },
              "to": {
                "$ref": "#/$defs/type_key"
              },
              "valid": {
                "type": "boolean",
                "description": "Opt-in validity axis (RFC-0031 §3.4)."
              },
              "description": {
                "type": "string"
              },
              "tags": {
                "$ref": "#/$defs/tags"
              }
            }
          }
        },
        "activity_types": {
          "type": "array",
          "items": {
            "type": "object",
            "required": [
              "key"
            ],
            "additionalProperties": false,
            "properties": {
              "key": {
                "$ref": "#/$defs/type_key"
              },
              "title": {
                "type": "string"
              },
              "is_task": {
                "type": "boolean"
              },
              "description": {
                "type": "string"
              },
              "tags": {
                "$ref": "#/$defs/tags"
              }
            }
          }
        }
      }
    },
    "contributes": {
      "type": "array",
      "description": "Manifest 0.3, oaap.data.model 0.1 §2.2: what THIS app writes into the tenant's twin.",
      "items": {
        "type": "object",
        "required": [
          "type"
        ],
        "additionalProperties": false,
        "properties": {
          "type": {
            "type": "string",
            "minLength": 1
          },
          "role": {
            "enum": [
              "owner",
              "contributor"
            ],
            "default": "owner"
          },
          "group": {
            "type": "string",
            "pattern": "^[a-z][a-z0-9]*(\\.[a-z][a-z0-9]*)+$"
          }
        },
        "if": {
          "properties": {
            "role": {
              "const": "contributor"
            }
          },
          "required": [
            "role"
          ]
        },
        "then": {
          "required": [
            "group"
          ]
        }
      }
    },
    "consumes": {
      "type": "array",
      "description": "Manifest 0.3, oaap.data.model 0.1 §2.2: what THIS app reads from the tenant's twin.",
      "items": {
        "type": "object",
        "required": [
          "type"
        ],
        "additionalProperties": false,
        "properties": {
          "type": {
            "type": "string",
            "minLength": 1
          },
          "as": {
            "type": "string",
            "description": "The app's own word for the type (RFC-0031 D4)."
          },
          "fields": {
            "type": "array",
            "items": {
              "type": "string"
            },
            "description": "Omit for reference-only (RFC-0031 D6)."
          }
        }
      }
    },
    "launchpad": {
      "type": "object",
      "additionalProperties": false,
      "description": "Manifest 0.4, RFC-0036 D1/D2: developer hints about the launchpad tile's DISPLAY, never about who may see it (that stays 'routes[].roles' and the operator-set 'visibility', RFC-0007). Not 'placement' above — that section is node PLACEMENT per RFC-0003 (GPU, site), an unrelated meaning of the same English word.",
      "properties": {
        "group": {
          "type": "string",
          "maxLength": 40,
          "description": "An optional section label; tiles sharing one are shown under one heading on the launchpad. Absent means the pre-existing, unlabelled section."
        },
        "embeddable": {
          "type": "boolean",
          "description": "Reserved; has no effect yet (RFC-0036 D1). Every tile opens its app as a standalone page today, per RFC-0016's network isolation. The name is reserved now so a future embedded shell mode needs no breaking manifest change."
        }
      }
    },
    "endpoints": {
      "type": "array",
      "maxItems": 1,
      "description": "RFC-0016/RFC-0015: at most one non-HTTP endpoint the app may declare; published only after an operator grants it on an 'exposed' node.",
      "items": {
        "type": "object",
        "required": [
          "name",
          "protocol",
          "container_port",
          "reason"
        ],
        "additionalProperties": false,
        "properties": {
          "name": {
            "type": "string",
            "pattern": "^[a-z0-9][a-z0-9-]*$"
          },
          "protocol": {
            "enum": [
              "udp",
              "tcp",
              "both"
            ]
          },
          "container_port": {
            "type": "integer",
            "minimum": 1,
            "maximum": 65535
          },
          "wish": {
            "type": "integer",
            "minimum": 1,
            "maximum": 65535,
            "description": "desired published host port; granted if free, otherwise the platform assigns another (ignored when fixed:true, where container_port IS the port)"
          },
          "fixed": {
            "type": "boolean",
            "description": "RFC-0017 §5.1: the port is a requirement, not a wish. Used by servers that advertise their own port to clients (a media server: ICE candidates carry the exact port). When true the endpoint is published unchanged (host_port == container_port), container_port MUST be in 8200-8299, and a grant fails loudly if the port is taken rather than reassigning."
          },
          "service": {
            "type": "string",
            "pattern": "^[a-z0-9][a-z0-9-]*$"
          },
          "reason": {
            "type": "string",
            "description": "Why this app needs a port past the gateway. Required, because the operator granting it reads this sentence and nothing else — and the schema listed it as required without ever allowing it, so no manifest with an endpoint could validate (found 2026-08-24)."
          }
        }
      }
    }
  },
  "$defs": {
    "type_key": {
      "type": "string",
      "pattern": "^[A-Za-z][A-Za-z0-9]*$",
      "$comment": "oaap.data.model 0.1 §2.2 — object, attribute, relation and activity type keys; mirrors appctl.py _MODEL_KEY_RE."
    },
    "tags": {
      "type": "array",
      "items": {
        "type": "string"
      },
      "$comment": "Presentation only, like title/description — appctl.py type_change_kind() never counts a change here as destructive."
    }
  }
}
